(window.webpackJsonp=window.webpackJsonp||[]).push([[48],{"2f289567f2dbf079020d":function(e,t){e.exports="<p>\u8c03\u7814\u76f8\u5173\u8f6f\u4ef6</p> <ul> <li>node-pty</li> <li>xterm.js</li> <li>blessed</li> <li>ssh2</li> </ul> <h3 id=node-pty>node-pty</h3> <p><a href=https://github.com/microsoft/node-pty>\u94fe\u63a5</a></p> <p>node.js \u7684 forkpty\uff083\uff09 \u7ed1\u5b9a\u3002\u8fd9\u5141\u8bb8\u60a8\u4f7f\u7528\u4f2a\u7ec8\u7aef\u6587\u4ef6\u63cf\u8ff0\u7b26\u5206\u53c9\u8fdb\u7a0b\u3002\u5b83\u8fd4\u56de\u5141\u8bb8\u8bfb\u53d6\u548c\u5199\u5165\u7684\u7ec8\u7aef\u5bf9\u8c61\u3002</p> <p>\u8fd9\u5bf9\u4e8e\uff1a</p> <ul> <li>\u7f16\u5199\u7ec8\u7aef\u4eff\u771f\u5668\uff08\u4f8b\u5982\u901a\u8fc7 xterm.js\uff09\u3002</li> <li>\u8ba9\u67d0\u4e9b\u7a0b\u5e8f\u8ba4\u4e3a\u60a8\u662f\u7ec8\u7aef\uff0c\u4f8b\u5982\u5f53\u60a8\u9700\u8981\u7a0b\u5e8f\u5411\u60a8\u53d1\u9001\u63a7\u5236\u5e8f\u5217\u65f6\u3002</li> </ul> <p>node-pty \u652f\u6301 Linux\u3001macOS \u548c Windows\u3002\u901a\u8fc7\u4f7f\u7528 Windows 1809+ \u4e0a\u7684 <a href=https://blogs.msdn.microsoft.com/commandline/2018/08/02/windows-command-line-introducing-the-windows-pseudo-console-conpty/ >Windows conpty API</a> \u6216\u8005\u65e7\u7248\u672c\u4e2d\u7684<a href=https://github.com/rprichard/winpty>winpty</a>\uff0c\u83b7\u5f97 Windows \u652f\u6301\u3002</p> <h3 id=xtermjs>xterm.js</h3> <p><a href=https://github.com/xtermjs/xterm.js>\u94fe\u63a5</a></p> <p>Xterm.js \u662f\u4e00\u4e2a\u5728 TypeScript \u4e2d\u7f16\u5199\u7684\u524d\u7aef\u7ec4\u4ef6\uff0c\u5b83\u5141\u8bb8\u5e94\u7528\u7a0b\u5e8f\u5728\u6d4f\u89c8\u5668\u4e2d\u4e3a\u5176\u7528\u6237\u5e26\u6765\u529f\u80fd\u9f50\u5168\u7684\u7ec8\u7aef\u3002\u5b83\u88ab\u6d41\u884c\u7684\u9879\u76ee\uff0c\u5982VS\u4ee3\u7801\uff0c\u8d85\u7ea7\u548cTheia\u4f7f\u7528\u3002</p> <h4 id=feature><strong>Feature</strong></h4> <ul> <li>Terminal apps just work: Xterm.js\u53ef\u4ee5\u901a\u5927\u591a\u6570\u7ec8\u7aefapps\uff0c\u6bd4\u5982<code>bash</code>\uff0c<code>vim</code>\u548c<code>tmux</code>\uff0c\u4e00\u8d77\u5de5\u4f5c\uff0c\u8fd9\u5305\u542b\u4e86Curses-based \u5e94\u7528\u548c\u9f20\u6807\u4e8b\u4ef6\u652f\u6301\u3002</li> <li>\u6027\u80fd\uff1aXterm.js \u975e\u5e38\u5feb\u901f\uff0c\u5b83\u751a\u81f3\u5305\u62ec\u4e86GPU\u52a0\u901f\u6e32\u67d3</li> <li>\u4e30\u5bcc\u7684unicode\u652f\u6301\uff1a\u652f\u6301CJK\uff0cemojis \u548cIMEs</li> <li>\u81ea\u5305\u542b\uff1a\u6ca1\u6709\u4efb\u4f55\u4f9d\u8d56\u5c31\u53ef\u4ee5\u8fd0\u884c</li> <li>\u65e0\u969c\u788d\uff1a\u53ef\u4ee5\u6253\u5f00\u5c4f\u5e55\u9605\u8bfb\u5668\u548c\u6700\u5c0f\u5bf9\u6bd4\u5ea6\u652f\u6301</li> <li>\u66f4\u591a\u7684\uff1aLinks\uff0ctherming\uff0caddons\uff0c\u4f18\u79c0\u7684API\u6587\u6863\uff0c\u7b49\u7b49</li> </ul> <h4 id=what-xtermjs-is-not>What xterm.js is not</h4> <ul> <li>Xterm.js is not a terminal application that you can download and use on your computer.</li> <li>Xterm.js is not <code>bash</code>. Xterm.js can be connected to processes like <code>bash</code> and let you interact with them (provide input, receive output).</li> </ul> <h3 id=blessed>blessed</h3> <p>\u4e3anode.js\u5f00\u53d1\u7684\u4e00\u4e2a\u5e26\u6709\u9ad8\u7ea7\u7ec8\u7aef\u63a5\u53e3\u7684curses-like library</p> <h3 id=ssh2>ssh2</h3> <p>SSH2 \u5ba2\u6237\u7aef\u548c\u670d\u52a1\u5668\u6a21\u5757\u4ee5\u7eaf JavaScript \u7f16\u5199\u7684\u8fd0\u884c\u4e8e node.js.</p> <p>\u5f00\u53d1/\u6d4b\u8bd5\u9488\u5bf9 OpenSSH\uff08\u5f53\u524d\u4e3a 7.6\uff09 \u5b8c\u6210\u3002</p> <h2 id=\u8bbe\u8ba1\u5b9e\u73b0>\u8bbe\u8ba1\u5b9e\u73b0</h2> <p>\u6700\u521d\u8bbe\u8ba1\u5b9e\u73b0\u662f\u901a\u8fc7websocket\u670d\u52a1\u6253\u901a\u6d4f\u89c8\u5668\u5230server\u4e4b\u95f4\u7684\u8fde\u63a5\uff0c\u4f7f\u7528<code>xterm.js</code>\u5728\u6d4f\u89c8\u5668\u521b\u5efaterminal UI\uff0c\u5e76\u4f7f\u7528<code>node-pty</code>\u5728server\u6253\u5f00\u5bf9\u5e94\u7684shell\u6765\u8fdb\u884c\u670d\u52a1\u3002</p> <p>\u8fd9\u6837\u53ea\u9700\u8981\u4f7f\u7528web\u505a\u597d\u8ba4\u8bc1\u5c31\u53ef\u4ee5\u5904\u7406\u5f53\u524d\u7684\u95ee\u9898\u3002</p> <p>\u7ecf\u8fc7\u4e00\u756a\u9009\u62e9\u540e\uff0c\u4e0d\u5e0c\u671b\u76f4\u63a5\u8fde\u63a5\u670d\u52a1\u5668\uff0c\u6240\u4ee5\u66f4\u6362\u4e86\u4e00\u79cd\u65b9\u5f0f\u3002</p> <p>\u5728web\u670d\u52a1\u5668\u4e0a\u542f\u52a8\u4e00\u4e2a<code>ss2</code>\u5ba2\u6237\u7aef\u4ee3\u7406\uff0c\u63a5\u5165\u670d\u52a1\u540e\uff0c\u8981\u6c42\u7528\u6237\u6765\u8f93\u5165\u7528\u6237\u7684ssh\u767b\u9646\u5bc6\u7801\u6765\u8fdb\u884c\u767b\u9646\uff0c\u5bf9\u5e94\u670d\u52a1\u5668\u4e0a\u9700\u8981\u542f\u52a8openssh-server\u3002\u5728\u6d4f\u89c8\u5668\u542f\u52a8xterm.js\u6a21\u62df\u7ec8\u7aef\uff0c\u4e4b\u540e\u5728\u6d4f\u89c8\u5668\u548c\u670d\u52a1\u4e4b\u95f4\u521b\u5efa\u8fde\u63a5\u96a7\u9053\uff0c\u8ba9\u7ec8\u7aef\u548cssh2\u4ee3\u7406\u901a\u8baf\u3002</p> <h2 id=ssh2\u89e3\u6790>SSH2\u89e3\u6790</h2> <pre class=hljs><code class=language-txt>ssh2@0.8.9\n\u2514\u2500\u252c ssh2-streams@0.4.10\n  \u251c\u2500\u252c asn1@0.2.4\n  \u2502 \u2514\u2500\u2500 safer-buffer@2.1.2\n  \u251c\u2500\u252c bcrypt-pbkdf@1.0.2\n  \u2502 \u2514\u2500\u2500 tweetnacl@0.14.5\n  \u2514\u2500\u2500 streamsearch@0.1.2</code></pre><ul> <li>ssh2 \u63d0\u4f9bssh Client/Server </li> <li>ssh-streams \u63d0\u4f9bssh\u4f20\u8f93\u6d41\uff0c\u5305\u542b\u52a0\u5bc6\u89e3\u5bc6\u548c\u4f20\u8f93\u5904\u7406</li> </ul> <h2 id=ssh2-api>SSH2 API</h2> <p><code>require(&#39;ssh2&#39;).Client</code> returns a <strong><em>Client</em></strong> constructor.</p> <p><code>require(&#39;ssh2&#39;).Server</code> returns a <strong><em>Server</em></strong> constructor.</p> <p><code>require(&#39;ssh2&#39;).utils</code> returns the <a href=https://github.com/mscdex/ssh2-streams#utility-methods>utility methods from <code>ssh2-streams</code></a>.</p> <p><code>require(&#39;ssh2&#39;).HTTPAgent</code> returns an <a href=https://nodejs.org/docs/latest/api/http.html#http_class_http_agent><code>http.Agent</code></a> constructor.</p> <p><code>require(&#39;ssh2&#39;).HTTPSAgent</code> returns an <a href=https://nodejs.org/docs/latest/api/https.html#https_class_https_agent><code>https.Agent</code></a> constructor. Its API is the same as <code>HTTPAgent</code> except it&#39;s for HTTPS connections.</p> <p><code>require(&#39;ssh2&#39;).SFTP_STATUS_CODE</code> returns the <a href=https://github.com/mscdex/ssh2-streams/blob/master/SFTPStream.md#sftpstream-static-constants><code>SFTPStream.STATUS_CODE</code> from <code>ssh2-streams</code></a>.</p> <p><code>require(&#39;ssh2&#39;).SFTP_OPEN_MODE</code> returns the <a href=https://github.com/mscdex/ssh2-streams/blob/master/SFTPStream.md#sftpstream-static-constants><code>SFTPStream.OPEN_MODE</code> from <code>ssh2-streams</code></a>.</p> <h3 id=client>Client</h3> <h4 id=client-events>Client events</h4> <ul> <li><p><strong>banner</strong>(&lt; <em>string</em> &gt;message, &lt; <em>string</em> &gt;language) - A notice was sent by the server upon connection.</p> </li> <li><p><strong>ready</strong>() - Authentication was successful.</p> </li> <li><p><strong>tcp connection</strong>(&lt; <em>object</em> &gt;details, &lt; <em>function</em> &gt;accept, &lt; <em>function</em> &gt;reject) - An incoming forwarded TCP connection is being requested. Calling <code>accept</code> accepts the connection and returns a <code>Channel</code> object. Calling <code>reject</code> rejects the connection and no further action is needed. <code>details</code> contains:</p> <ul> <li><p><strong>srcIP</strong> - <em>string</em> - The originating IP of the connection.</p> </li> <li><p><strong>srcPort</strong> - <em>integer</em> - The originating port of the connection.</p> </li> <li><p><strong>destIP</strong> - <em>string</em> - The remote IP the connection was received on (given in earlier call to <code>forwardIn()</code>).</p> </li> <li><p><strong>destPort</strong> - <em>integer</em> - The remote port the connection was received on (given in earlier call to <code>forwardIn()</code>).</p> </li> </ul> </li> <li><p><strong>x11</strong>(&lt; <em>object</em> &gt;details, &lt; <em>function</em> &gt;accept, &lt; <em>function</em> &gt;reject) - An incoming X11 connection is being requested. Calling <code>accept</code> accepts the connection and returns a <code>Channel</code> object. Calling <code>reject</code> rejects the connection and no further action is needed. <code>details</code> contains:</p> <ul> <li><p><strong>srcIP</strong> - <em>string</em> - The originating IP of the connection.</p> </li> <li><p><strong>srcPort</strong> - <em>integer</em> - The originating port of the connection.</p> </li> </ul> </li> <li><p><strong>keyboard-interactive</strong>(&lt; <em>string</em> &gt;name, &lt; <em>string</em> &gt;instructions, &lt; <em>string</em> &gt;instructionsLang, &lt; <em>array</em> &gt;prompts, &lt; <em>function</em> &gt;finish) - The server is asking for replies to the given <code>prompts</code> for keyboard-interactive user authentication. <code>name</code> is generally what you&#39;d use as a window title (for GUI apps). <code>prompts</code> is an array of <code>{ prompt: &#39;Password: &#39;, echo: false }</code> style objects (here <code>echo</code> indicates whether user input should be displayed on the screen). The answers for all prompts must be provided as an array of strings and passed to <code>finish</code> when you are ready to continue. Note: It&#39;s possible for the server to come back and ask more questions.</p> </li> <li><p><strong>unix connection</strong>(&lt; <em>object</em> &gt;details, &lt; <em>function</em> &gt;accept, &lt; <em>function</em> &gt;reject) - An incoming forwarded UNIX socket connection is being requested. Calling <code>accept</code> accepts the connection and returns a <code>Channel</code> object. Calling <code>reject</code> rejects the connection and no further action is needed. <code>details</code> contains:</p> <ul> <li><strong>socketPath</strong> - <em>string</em> - The originating UNIX socket path of the connection.</li> </ul> </li> <li><p><strong>change password</strong>(&lt; <em>string</em> &gt;message, &lt; <em>string</em> &gt;language, &lt; <em>function</em> &gt;done) - If using password-based user authentication, the server has requested that the user&#39;s password be changed. Call <code>done</code> with the new password.</p> </li> <li><p><strong>continue</strong>() - Emitted when more requests/data can be sent to the server (after a <code>Client</code> method returned <code>false</code>).</p> </li> <li><p><strong>error</strong>(&lt; <em>Error</em> &gt;err) - An error occurred. A &#39;level&#39; property indicates &#39;client-socket&#39; for socket-level errors and &#39;client-ssh&#39; for SSH disconnection messages. In the case of &#39;client-ssh&#39; messages, there may be a &#39;description&#39; property that provides more detail.</p> </li> <li><p><strong>end</strong>() - The socket was disconnected.</p> </li> <li><p><strong>close</strong>() - The socket was closed.</p> </li> </ul> <h4 id=client-methods>Client methods</h4> <ul> <li><p><strong>(constructor)</strong>() - Creates and returns a new Client instance.</p> </li> <li><p><strong>connect</strong>(&lt; <em>object</em> &gt;config) - <em>(void)</em> - Attempts a connection to a server using the information given in <code>config</code>:</p> <ul> <li><p><strong>host</strong> - <em>string</em> - Hostname or IP address of the server. <strong>Default:</strong> <code>&#39;localhost&#39;</code></p> </li> <li><p><strong>port</strong> - <em>integer</em> - Port number of the server. <strong>Default:</strong> <code>22</code></p> </li> <li><p><strong>localAddress</strong> - <em>string</em> - IP address of the network interface to use to connect to the server. <strong>Default:</strong> (none -- determined by OS)</p> </li> <li><p><strong>localPort</strong> - <em>string</em> - The local port number to connect from. <strong>Default:</strong> (none -- determined by OS)</p> </li> <li><p><strong>forceIPv4</strong> - <em>boolean</em> - Only connect via resolved IPv4 address for <code>host</code>. <strong>Default:</strong> <code>false</code></p> </li> <li><p><strong>forceIPv6</strong> - <em>boolean</em> - Only connect via resolved IPv6 address for <code>host</code>. <strong>Default:</strong> <code>false</code></p> </li> <li><p><strong>hostHash</strong> - <em>string</em> - Any valid hash algorithm supported by node. The host&#39;s key is hashed using this algorithm and passed to the <strong>hostVerifier</strong> function. <strong>Default:</strong> (none)</p> </li> <li><p><strong>hostVerifier</strong> - <em>function</em> - Function with parameters <code>(hashedKey[, callback])</code> where <code>hashedKey</code> is a string hex hash of the host&#39;s key for verification purposes. Return <code>true</code> to continue with the handshake or <code>false</code> to reject and disconnect, or call <code>callback()</code> with <code>true</code> or <code>false</code> if you need to perform asynchronous verification. <strong>Default:</strong> (auto-accept if <code>hostVerifier</code> is not set)</p> </li> <li><p><strong>username</strong> - <em>string</em> - Username for authentication. <strong>Default:</strong> (none)</p> </li> <li><p><strong>password</strong> - <em>string</em> - Password for password-based user authentication. <strong>Default:</strong> (none)</p> </li> <li><p><strong>agent</strong> - <em>string</em> - Path to ssh-agent&#39;s UNIX socket for ssh-agent-based user authentication. <strong>Windows users: set to &#39;pageant&#39; for authenticating with Pageant or (actual) path to a cygwin &quot;UNIX socket.&quot;</strong> <strong>Default:</strong> (none)</p> </li> <li><p><strong>agentForward</strong> - <em>boolean</em> - Set to <code>true</code> to use OpenSSH agent forwarding (<code>auth-agent@openssh.com</code>) for the life of the connection. <code>agent</code> must also be set to use this feature. <strong>Default:</strong> <code>false</code></p> </li> <li><p><strong>privateKey</strong> - <em>mixed</em> - <em>Buffer</em> or <em>string</em> that contains a private key for either key-based or hostbased user authentication (OpenSSH format). <strong>Default:</strong> (none)</p> </li> <li><p><strong>passphrase</strong> - <em>string</em> - For an encrypted private key, this is the passphrase used to decrypt it. <strong>Default:</strong> (none)</p> </li> <li><p><strong>localHostname</strong> - <em>string</em> - Along with <strong>localUsername</strong> and <strong>privateKey</strong>, set this to a non-empty string for hostbased user authentication. <strong>Default:</strong> (none)</p> </li> <li><p><strong>localUsername</strong> - <em>string</em> - Along with <strong>localHostname</strong> and <strong>privateKey</strong>, set this to a non-empty string for hostbased user authentication. <strong>Default:</strong> (none)</p> </li> <li><p><strong>tryKeyboard</strong> - <em>boolean</em> - Try keyboard-interactive user authentication if primary user authentication method fails. If you set this to <code>true</code>, you need to handle the <code>keyboard-interactive</code> event. <strong>Default:</strong> <code>false</code></p> </li> <li><p><strong>authHandler</strong> - <em>function</em> - Function with parameters <code>(methodsLeft, partialSuccess, callback)</code> where <code>methodsLeft</code> and <code>partialSuccess</code> are <code>null</code> on the first authentication attempt, otherwise are an array and boolean respectively. Return or call <code>callback()</code> with the name of the authentication method to try next (pass <code>false</code> to signal no more methods to try). Valid method names are: <code>&#39;none&#39;, &#39;password&#39;, &#39;publickey&#39;, &#39;agent&#39;, &#39;keyboard-interactive&#39;, &#39;hostbased&#39;</code>. <strong>Default:</strong> function that follows a set method order: None -&gt; Password -&gt; Private Key -&gt; Agent (-&gt; keyboard-interactive if <code>tryKeyboard</code> is <code>true</code>) -&gt; Hostbased</p> </li> <li><p><strong>keepaliveInterval</strong> - <em>integer</em> - How often (in milliseconds) to send SSH-level keepalive packets to the server (in a similar way as OpenSSH&#39;s ServerAliveInterval config option). Set to 0 to disable. <strong>Default:</strong> <code>0</code></p> </li> <li><p><strong>keepaliveCountMax</strong> - <em>integer</em> - How many consecutive, unanswered SSH-level keepalive packets that can be sent to the server before disconnection (similar to OpenSSH&#39;s ServerAliveCountMax config option). <strong>Default:</strong> <code>3</code></p> </li> <li><p><strong>readyTimeout</strong> - <em>integer</em> - How long (in milliseconds) to wait for the SSH handshake to complete. <strong>Default:</strong> <code>20000</code></p> </li> <li><p><strong>sock</strong> - <em>ReadableStream</em> - A <em>ReadableStream</em> to use for communicating with the server instead of creating and using a new TCP connection (useful for connection hopping).</p> </li> <li><p><strong>strictVendor</strong> - <em>boolean</em> - Performs a strict server vendor check before sending vendor-specific requests, etc. (e.g. check for OpenSSH server when using <code>openssh_noMoreSessions()</code>) <strong>Default:</strong> <code>true</code></p> </li> <li><p><strong>algorithms</strong> - <em>object</em> - This option allows you to explicitly override the default transport layer algorithms used for the connection. Each value must be an array of valid algorithms for that category. The order of the algorithms in the arrays are important, with the most favorable being first. For a list of valid and default algorithm names, please review the documentation for the version of <code>ssh2-streams</code> used by this module. Valid keys:</p> <ul> <li><p><strong>kex</strong> - <em>array</em> - Key exchange algorithms.</p> </li> <li><p><strong>cipher</strong> - <em>array</em> - Ciphers.</p> </li> <li><p><strong>serverHostKey</strong> - <em>array</em> - Server host key formats.</p> </li> <li><p><strong>hmac</strong> - <em>array</em> - (H)MAC algorithms.</p> </li> <li><p><strong>compress</strong> - <em>array</em> - Compression algorithms.</p> </li> </ul> </li> <li><p><strong>compress</strong> - <em>mixed</em> - Set to <code>true</code> to enable compression if server supports it, <code>&#39;force&#39;</code> to force compression (disconnecting if server does not support it), or <code>false</code> to explicitly opt out of compression all of the time. Note: this setting is overridden when explicitly setting a compression algorithm in the <code>algorithms</code> configuration option. <strong>Default:</strong> (only use compression if that is only what the server supports)</p> </li> <li><p><strong>debug</strong> - <em>function</em> - Set this to a function that receives a single string argument to get detailed (local) debug information. <strong>Default:</strong> (none)</p> </li> </ul> </li> </ul> <p><strong>Default authentication method order:</strong> None -&gt; Password -&gt; Private Key -&gt; Agent (-&gt; keyboard-interactive if <code>tryKeyboard</code> is <code>true</code>) -&gt; Hostbased</p> <ul> <li><p><strong>exec</strong>(&lt; <em>string</em> &gt;command[, &lt; <em>object</em> &gt;options], &lt; <em>function</em> &gt;callback) - <em>boolean</em> - Executes <code>command</code> on the server. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic. <code>callback</code> has 2 parameters: &lt; <em>Error</em> &gt;err, &lt; <em>Channel</em> &gt;stream. Valid <code>options</code> properties are:</p> <ul> <li><p><strong>env</strong> - <em>object</em> - An environment to use for the execution of the command.</p> </li> <li><p><strong>pty</strong> - <em>mixed</em> - Set to <code>true</code> to allocate a pseudo-tty with defaults, or an object containing specific pseudo-tty settings (see &#39;Pseudo-TTY settings&#39;). Setting up a pseudo-tty can be useful when working with remote processes that expect input from an actual terminal (e.g. sudo&#39;s password prompt).</p> </li> <li><p><strong>x11</strong> - <em>mixed</em> - Set to <code>true</code> to use defaults below, set to a number to specify a specific screen number, or an object with the following valid properties:</p> <ul> <li><p><strong>single</strong> - <em>boolean</em> - Allow just a single connection? <strong>Default:</strong> <code>false</code></p> </li> <li><p><strong>screen</strong> - <em>number</em> - Screen number to use <strong>Default:</strong> <code>0</code></p> </li> <li><p><strong>protocol</strong> - <em>string</em> - The authentication protocol name. <strong>Default:</strong> <code>&#39;MIT-MAGIC-COOKIE-1&#39;</code></p> </li> <li><p><strong>cookie</strong> - <em>mixed</em> - The authentication cookie. Can be a hex <em>string</em> or a <em>Buffer</em> containing the raw cookie value (which will be converted to a hex string). <strong>Default:</strong> (random 16 byte value)</p> </li> </ul> </li> </ul> </li> <li><p><strong>shell</strong>([[&lt; <em>mixed</em> &gt;window,] &lt; <em>object</em> &gt;options]&lt; <em>function</em> &gt;callback) - <em>boolean</em> - Starts an interactive shell session on the server, with an optional <code>window</code> object containing pseudo-tty settings (see &#39;Pseudo-TTY settings&#39;). If <code>window === false</code>, then no pseudo-tty is allocated. <code>options</code> supports the <code>x11</code> and <code>env</code> options as described in <code>exec()</code>. <code>callback</code> has 2 parameters: &lt; <em>Error</em> &gt;err, &lt; <em>Channel</em> &gt;stream. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>forwardIn</strong>(&lt; <em>string</em> &gt;remoteAddr, &lt; <em>integer</em> &gt;remotePort, &lt; <em>function</em> &gt;callback) - <em>boolean</em> - Bind to <code>remoteAddr</code> on <code>remotePort</code> on the server and forward incoming TCP connections. <code>callback</code> has 2 parameters: &lt; <em>Error</em> &gt;err, &lt; <em>integer</em> &gt;port (<code>port</code> is the assigned port number if <code>remotePort</code> was 0). Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic. Here are some special values for <code>remoteAddr</code> and their associated binding behaviors:</p> <ul> <li><p>&#39;&#39; - Connections are to be accepted on all protocol families supported by the server.</p> </li> <li><p>&#39;0.0.0.0&#39; - Listen on all IPv4 addresses.</p> </li> <li><p>&#39;::&#39; - Listen on all IPv6 addresses.</p> </li> <li><p>&#39;localhost&#39; - Listen on all protocol families supported by the server on loopback addresses only.</p> </li> <li><p>&#39;127.0.0.1&#39; and &#39;::1&#39; - Listen on the loopback interfaces for IPv4 and IPv6, respectively.</p> </li> </ul> </li> <li><p><strong>unforwardIn</strong>(&lt; <em>string</em> &gt;remoteAddr, &lt; <em>integer</em> &gt;remotePort, &lt; <em>function</em> &gt;callback) - <em>boolean</em> - Unbind from <code>remoteAddr</code> on <code>remotePort</code> on the server and stop forwarding incoming TCP connections. Until <code>callback</code> is called, more connections may still come in. <code>callback</code> has 1 parameter: &lt; <em>Error</em> &gt;err. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>forwardOut</strong>(&lt; <em>string</em> &gt;srcIP, &lt; <em>integer</em> &gt;srcPort, &lt; <em>string</em> &gt;dstIP, &lt; <em>integer</em> &gt;dstPort, &lt; <em>function</em> &gt;callback) - <em>boolean</em> - Open a connection with <code>srcIP</code> and <code>srcPort</code> as the originating address and port and <code>dstIP</code> and <code>dstPort</code> as the remote destination address and port. <code>callback</code> has 2 parameters: &lt; <em>Error</em> &gt;err, &lt; <em>Channel</em> &gt;stream. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>sftp</strong>(&lt; <em>function</em> &gt;callback) - <em>boolean</em> - Starts an SFTP session. <code>callback</code> has 2 parameters: &lt; <em>Error</em> &gt;err, &lt; <em>SFTPStream</em> &gt;sftp. For methods available on <code>sftp</code>, see the <a href=https://github.com/mscdex/ssh2-streams/blob/master/SFTPStream.md><code>SFTPStream</code> client documentation</a> (except <code>read()</code> and <code>write()</code> are used instead of <code>readData()</code> and <code>writeData()</code> respectively, for convenience). Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>subsys</strong>(&lt; <em>string</em> &gt;subsystem, &lt; <em>function</em> &gt;callback) - <em>boolean</em> - Invokes <code>subsystem</code> on the server. <code>callback</code> has 2 parameters: &lt; <em>Error</em> &gt;err, &lt; <em>Channel</em> &gt;stream. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>end</strong>() - <em>(void)</em> - Disconnects the socket.</p> </li> <li><p><strong>openssh_noMoreSessions</strong>(&lt; <em>function</em> &gt;callback) - <em>boolean</em> - OpenSSH extension that sends a request to reject any new sessions (e.g. exec, shell, sftp, subsys) for this connection. <code>callback</code> has 1 parameter: &lt; <em>Error</em> &gt;err. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>openssh_forwardInStreamLocal</strong>(&lt; <em>string</em> &gt;socketPath, &lt; <em>function</em> &gt;callback) - <em>boolean</em> - OpenSSH extension that binds to a UNIX domain socket at <code>socketPath</code> on the server and forwards incoming connections. <code>callback</code> has 1 parameter: &lt; <em>Error</em> &gt;err. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>openssh_unforwardInStreamLocal</strong>(&lt; <em>string</em> &gt;socketPath, &lt; <em>function</em> &gt;callback) - <em>boolean</em> - OpenSSH extension that unbinds from a UNIX domain socket at <code>socketPath</code> on the server and stops forwarding incoming connections. <code>callback</code> has 1 parameter: &lt; <em>Error</em> &gt;err. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>openssh_forwardOutStreamLocal</strong>(&lt; <em>string</em> &gt;socketPath, &lt; <em>function</em> &gt;callback) - <em>boolean</em> - OpenSSH extension that opens a connection to a UNIX domain socket at <code>socketPath</code> on the server. <code>callback</code> has 2 parameters: &lt; <em>Error</em> &gt;err, &lt; <em>Channel</em> &gt;stream. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> </ul> <h3 id=server>Server</h3> <h4 id=server-events>Server events</h4> <ul> <li><p><strong>connection</strong>(&lt; <em>Connection</em> &gt;client, &lt; <em>object</em> &gt;info) - A new client has connected. <code>info</code> contains the following properties:</p> <ul> <li><p><strong>ip</strong> - <em>string</em> - The <code>remoteAddress</code> of the connection.</p> </li> <li><p><strong>family</strong> - <em>string</em> - The <code>remoteFamily</code> of the connection.</p> </li> <li><p><strong>port</strong> - <em>integer</em> - The <code>remotePort</code> of the connection.</p> </li> <li><p><strong>header</strong> - <em>object</em> - Information about the client&#39;s header:</p> <ul> <li><p><strong>identRaw</strong> - <em>string</em> - The raw client identification string.</p> </li> <li><p><strong>versions</strong> - <em>object</em> - Various version information:</p> <ul> <li><p><strong>protocol</strong> - <em>string</em> - The SSH protocol version (always <code>1.99</code> or <code>2.0</code>).</p> </li> <li><p><strong>software</strong> - <em>string</em> - The software name and version of the client.</p> </li> </ul> </li> <li><p><strong>comments</strong> - <em>string</em> - Any text that comes after the software name/version.</p> </li> </ul> <p>Example: the identification string <code>SSH-2.0-OpenSSH_6.6.1p1 Ubuntu-2ubuntu2</code> would be parsed as:</p> </li> </ul> </li> </ul> <pre class=hljs><code class=language-js>        { <span class=hljs-attr>identRaw</span>: <span class=hljs-string>'SSH-2.0-OpenSSH_6.6.1p1 Ubuntu-2ubuntu2'</span>,\n          <span class=hljs-attr>version</span>: {\n            <span class=hljs-attr>protocol</span>: <span class=hljs-string>'2.0'</span>,\n            <span class=hljs-attr>software</span>: <span class=hljs-string>'OpenSSH_6.6.1p1'</span>\n          },\n          <span class=hljs-attr>comments</span>: <span class=hljs-string>'Ubuntu-2ubuntu2'</span> }</code></pre><h4 id=server-methods>Server methods</h4> <ul> <li><p><strong>(constructor)</strong>(&lt; <em>object</em> &gt;config[, &lt; <em>function</em> &gt;connectionListener]) - Creates and returns a new Server instance. Server instances also have the same methods/properties/events as <a href=http://nodejs.org/docs/latest/api/net.html#net_class_net_server><code>net.Server</code></a>. <code>connectionListener</code> if supplied, is added as a <code>connection</code> listener. Valid <code>config</code> properties:</p> <ul> <li><p><strong>hostKeys</strong> - <em>array</em> - An array of either Buffers/strings that contain host private keys or objects in the format of <code>{ key: &lt;Buffer/string&gt;, passphrase: &lt;string&gt; }</code> for encrypted private keys. (<strong>Required</strong>) <strong>Default:</strong> (none)</p> </li> <li><p><strong>algorithms</strong> - <em>object</em> - This option allows you to explicitly override the default transport layer algorithms used for incoming client connections. Each value must be an array of valid algorithms for that category. The order of the algorithms in the arrays are important, with the most favorable being first. For a list of valid and default algorithm names, please review the documentation for the version of <code>ssh2-streams</code> used by this module. Valid keys:</p> <ul> <li><p><strong>kex</strong> - <em>array</em> - Key exchange algorithms.</p> </li> <li><p><strong>cipher</strong> - <em>array</em> - Ciphers.</p> </li> <li><p><strong>serverHostKey</strong> - <em>array</em> - Server host key formats.</p> </li> <li><p><strong>hmac</strong> - <em>array</em> - (H)MAC algorithms.</p> </li> <li><p><strong>compress</strong> - <em>array</em> - Compression algorithms.</p> </li> </ul> </li> <li><p><strong>greeting</strong> - <em>string</em> - A message that is sent to clients immediately upon connection, before handshaking begins. <strong>Note:</strong> Most clients usually ignore this. <strong>Default:</strong> (none)</p> </li> <li><p><strong>banner</strong> - <em>string</em> - A message that is sent to clients once, right before authentication begins. <strong>Default:</strong> (none)</p> </li> <li><p><strong>ident</strong> - <em>string</em> - A custom server software name/version identifier. <strong>Default:</strong> <code>&#39;ssh2js&#39; + moduleVersion + &#39;srv&#39;</code></p> </li> <li><p><strong>highWaterMark</strong> - <em>integer</em> - This is the <code>highWaterMark</code> to use for the parser stream. <strong>Default:</strong> <code>32 * 1024</code></p> </li> <li><p><strong>debug</strong> - <em>function</em> - Set this to a function that receives a single string argument to get detailed (local) debug information. <strong>Default:</strong> (none)</p> </li> </ul> </li> </ul> <h4 id=connection-events>Connection events</h4> <ul> <li><p><strong>authentication</strong>(&lt; <em>AuthContext</em> &gt;ctx) - The client has requested authentication. <code>ctx.username</code> contains the client username, <code>ctx.method</code> contains the requested authentication method, and <code>ctx.accept()</code> and <code>ctx.reject([&lt; Array &gt;authMethodsLeft[, &lt; Boolean &gt;isPartialSuccess]])</code> are used to accept or reject the authentication request respectively. <code>abort</code> is emitted if the client aborts the authentication request. Other properties/methods available on <code>ctx</code> depends on the <code>ctx.method</code> of authentication the client has requested:</p> <ul> <li><p><code>password</code>:</p> <ul> <li><strong>password</strong> - <em>string</em> - This is the password sent by the client.</li> </ul> </li> <li><p><code>publickey</code>:</p> <ul> <li><p><strong>key</strong> - <em>object</em> - Contains information about the public key sent by the client:</p> <ul> <li><p><strong>algo</strong> - <em>string</em> - The name of the key algorithm (e.g. <code>ssh-rsa</code>).</p> </li> <li><p><strong>data</strong> - <em>Buffer</em> - The actual key data.</p> </li> </ul> </li> <li><p><strong>sigAlgo</strong> - <em>mixed</em> - If the value is <code>undefined</code>, the client is only checking the validity of the <code>key</code>. If the value is a <em>string</em>, then this contains the signature algorithm that is passed to <a href=http://nodejs.org/docs/latest/api/crypto.html#crypto_crypto_createverify_algorithm><code>crypto.createVerify()</code></a>.</p> </li> <li><p><strong>blob</strong> - <em>mixed</em> - If the value is <code>undefined</code>, the client is only checking the validity of the <code>key</code>. If the value is a <em>Buffer</em>, then this contains the data that is passed to <a href=http://nodejs.org/docs/latest/api/crypto.html#crypto_verifier_update_data><code>verifier.update()</code></a>.</p> </li> <li><p><strong>signature</strong> - <em>mixed</em> - If the value is <code>undefined</code>, the client is only checking the validity of the <code>key</code>. If the value is a <em>Buffer</em>, then this contains a signature that is passed to <a href=http://nodejs.org/docs/latest/api/crypto.html#crypto_verifier_verify_object_signature_signature_format><code>verifier.verify()</code></a>.</p> </li> </ul> </li> <li><p><code>keyboard-interactive</code>:</p> <ul> <li><p><strong>submethods</strong> - <em>array</em> - A list of preferred authentication &quot;sub-methods&quot; sent by the client. This may be used to determine what (if any) prompts to send to the client.</p> </li> <li><p><strong>prompt</strong>(&lt; <em>array</em> &gt;prompts[, &lt; <em>string</em> &gt;title[, &lt; <em>string</em> &gt;instructions]], &lt; <em>function</em> &gt;callback) - <em>boolean</em> - Send prompts to the client. <code>prompts</code> is an array of <code>{ prompt: &#39;Prompt text&#39;, echo: true }</code> objects (<code>prompt</code> being the prompt text and <code>echo</code> indicating whether the client&#39;s response to the prompt should be echoed to their display). <code>callback</code> is called with <code>(err, responses)</code>, where <code>responses</code> is an array of string responses matching up to the <code>prompts</code>.</p> </li> </ul> </li> </ul> </li> <li><p><strong>ready</strong>() - Emitted when the client has been successfully authenticated.</p> </li> <li><p><strong>session</strong>(&lt; <em>function</em> &gt;accept, &lt; <em>function</em> &gt;reject) - Emitted when the client has requested a new session. Sessions are used to start interactive shells, execute commands, request X11 forwarding, etc. <code>accept()</code> returns a new <em>Session</em> instance. <code>reject()</code> Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>tcpip</strong>(&lt; <em>function</em> &gt;accept, &lt; <em>function</em> &gt;reject, &lt; <em>object</em> &gt;info) - Emitted when the client has requested an outbound (TCP) connection. <code>accept()</code> returns a new <em>Channel</em> instance representing the connection. <code>reject()</code> Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic. <code>info</code> contains:</p> <ul> <li><p><strong>srcIP</strong> - <em>string</em> - Source IP address of outgoing connection.</p> </li> <li><p><strong>srcPort</strong> - <em>string</em> - Source port of outgoing connection.</p> </li> <li><p><strong>destIP</strong> - <em>string</em> - Destination IP address of outgoing connection.</p> </li> <li><p><strong>destPort</strong> - <em>string</em> - Destination port of outgoing connection.</p> </li> </ul> </li> <li><p><strong>openssh.streamlocal</strong>(&lt; <em>function</em> &gt;accept, &lt; <em>function</em> &gt;reject, &lt; <em>object</em> &gt;info) - Emitted when the client has requested a connection to a UNIX domain socket. <code>accept()</code> returns a new <em>Channel</em> instance representing the connection. <code>reject()</code> Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic. <code>info</code> contains:</p> <ul> <li><strong>socketPath</strong> - <em>string</em> - Destination socket path of outgoing connection.</li> </ul> </li> <li><p><strong>request</strong>(&lt; <em>mixed</em> &gt;accept, &lt; <em>mixed</em> &gt;reject, &lt; <em>string</em> &gt;name, &lt; <em>object</em> &gt;info) - Emitted when the client has sent a global request for <code>name</code> (e.g. <code>tcpip-forward</code> or <code>cancel-tcpip-forward</code>). <code>accept</code> and <code>reject</code> are functions if the client requested a response. If <code>bindPort === 0</code>, you should pass the chosen port to <code>accept()</code> so that the client will know what port was bound. <code>info</code> contains additional details about the request:</p> <ul> <li><p><code>tcpip-forward</code> and <code>cancel-tcpip-forward</code>:</p> <ul> <li><p><strong>bindAddr</strong> - <em>string</em> - The IP address to start/stop binding to.</p> </li> <li><p><strong>bindPort</strong> - <em>integer</em> - The port to start/stop binding to.</p> </li> </ul> </li> <li><p><code>streamlocal-forward@openssh.com</code> and <code>cancel-streamlocal-forward@openssh.com</code>:</p> <ul> <li><strong>socketPath</strong> - <em>string</em> - The socket path to start/stop binding to.</li> </ul> </li> </ul> </li> <li><p><strong>rekey</strong>() - Emitted when the client has finished rekeying (either client or server initiated).</p> </li> <li><p><strong>continue</strong>() - Emitted when more requests/data can be sent to the client (after a <code>Connection</code> method returned <code>false</code>).</p> </li> <li><p><strong>error</strong>(&lt; <em>Error</em> &gt;err) - An error occurred.</p> </li> <li><p><strong>end</strong>() - The client socket disconnected.</p> </li> <li><p><strong>close</strong>() - The client socket was closed.</p> </li> </ul> <h4 id=connection-methods>Connection methods</h4> <ul> <li><p><strong>end</strong>() - <em>boolean</em> - Closes the client connection. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>x11</strong>(&lt; <em>string</em> &gt;originAddr, &lt; <em>integer</em> &gt;originPort, &lt; <em>function</em> &gt;callback) - <em>boolean</em> - Alert the client of an incoming X11 client connection from <code>originAddr</code> on port <code>originPort</code>. <code>callback</code> has 2 parameters: &lt; <em>Error</em> &gt;err, &lt; <em>Channel</em> &gt;stream. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>forwardOut</strong>(&lt; <em>string</em> &gt;boundAddr, &lt; <em>integer</em> &gt;boundPort, &lt; <em>string</em> &gt;remoteAddr, &lt; <em>integer</em> &gt;remotePort, &lt; <em>function</em> &gt;callback) - <em>boolean</em> - Alert the client of an incoming TCP connection on <code>boundAddr</code> on port <code>boundPort</code> from <code>remoteAddr</code> on port <code>remotePort</code>. <code>callback</code> has 2 parameters: &lt; <em>Error</em> &gt;err, &lt; <em>Channel</em> &gt;stream. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>openssh_forwardOutStreamLocal</strong>(&lt; <em>string</em> &gt;socketPath, &lt; <em>function</em> &gt;callback) - <em>boolean</em> - Alert the client of an incoming UNIX domain socket connection on <code>socketPath</code>. <code>callback</code> has 2 parameters: &lt; <em>Error</em> &gt;err, &lt; <em>Channel</em> &gt;stream. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>rekey</strong>([&lt; <em>function</em> &gt;callback]) - <em>boolean</em> - Initiates a rekeying with the client. If <code>callback</code> is supplied, it is added as a one-time handler for the <code>rekey</code> event. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> </ul> <h4 id=session-events>Session events</h4> <ul> <li><p><strong>pty</strong>(&lt; <em>mixed</em> &gt;accept, &lt; <em>mixed</em> &gt;reject, &lt; <em>object</em> &gt;info) - The client requested allocation of a pseudo-TTY for this session. <code>accept</code> and <code>reject</code> are functions if the client requested a response and return <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic. <code>info</code> has these properties:</p> <ul> <li><p><strong>cols</strong> - <em>integer</em> - The number of columns for the pseudo-TTY.</p> </li> <li><p><strong>rows</strong> - <em>integer</em> - The number of rows for the pseudo-TTY.</p> </li> <li><p><strong>width</strong> - <em>integer</em> - The width of the pseudo-TTY in pixels.</p> </li> <li><p><strong>height</strong> - <em>integer</em> - The height of the pseudo-TTY in pixels.</p> </li> <li><p><strong>modes</strong> - <em>object</em> - Contains the requested terminal modes of the pseudo-TTY keyed on the mode name with the value being the mode argument. (See the table at the end for valid names).</p> </li> </ul> </li> <li><p><strong>window-change</strong>(&lt; <em>mixed</em> &gt;accept, &lt; <em>mixed</em> &gt;reject, &lt; <em>object</em> &gt;info) - The client reported a change in window dimensions during this session. <code>accept</code> and <code>reject</code> are functions if the client requested a response and return <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic. <code>info</code> has these properties:</p> <ul> <li><p><strong>cols</strong> - <em>integer</em> - The new number of columns for the client window.</p> </li> <li><p><strong>rows</strong> - <em>integer</em> - The new number of rows for the client window.</p> </li> <li><p><strong>width</strong> - <em>integer</em> - The new width of the client window in pixels.</p> </li> <li><p><strong>height</strong> - <em>integer</em> - The new height of the client window in pixels.</p> </li> </ul> </li> <li><p><strong>x11</strong>(&lt; <em>mixed</em> &gt;accept, &lt; <em>mixed</em> &gt;reject, &lt; <em>object</em> &gt;info) - The client requested X11 forwarding. <code>accept</code> and <code>reject</code> are functions if the client requested a response and return <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic. <code>info</code> has these properties:</p> <ul> <li><p><strong>single</strong> - <em>boolean</em> - <code>true</code> if only a single connection should be forwarded.</p> </li> <li><p><strong>protocol</strong> - <em>string</em> - The name of the X11 authentication method used (e.g. <code>MIT-MAGIC-COOKIE-1</code>).</p> </li> <li><p><strong>cookie</strong> - <em>string</em> - The X11 authentication cookie encoded in hexadecimal.</p> </li> <li><p><strong>screen</strong> - <em>integer</em> - The screen number to forward X11 connections for.</p> </li> </ul> </li> <li><p><strong>env</strong>(&lt; <em>mixed</em> &gt;accept, &lt; <em>mixed</em> &gt;reject, &lt; <em>object</em> &gt;info) - The client requested an environment variable to be set for this session. <code>accept</code> and <code>reject</code> are functions if the client requested a response and return <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic. <code>info</code> has these properties:</p> <ul> <li><p><strong>key</strong> - <em>string</em> - The environment variable&#39;s name.</p> </li> <li><p><strong>value</strong> - <em>string</em> - The environment variable&#39;s value.</p> </li> </ul> </li> <li><p><strong>signal</strong>(&lt; <em>mixed</em> &gt;accept, &lt; <em>mixed</em> &gt;reject, &lt; <em>object</em> &gt;info) - The client has sent a signal. <code>accept</code> and <code>reject</code> are functions if the client requested a response and return <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic. <code>info</code> has these properties:</p> <ul> <li><strong>name</strong> - <em>string</em> - The signal name (e.g. <code>SIGUSR1</code>).</li> </ul> </li> <li><p><strong>auth-agent</strong>(&lt; <em>mixed</em> &gt;accept, &lt; <em>mixed</em> &gt;reject) - The client has requested incoming ssh-agent requests be forwarded to them. <code>accept</code> and <code>reject</code> are functions if the client requested a response and return <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>shell</strong>(&lt; <em>mixed</em> &gt;accept, &lt; <em>mixed</em> &gt;reject) - The client has requested an interactive shell. <code>accept</code> and <code>reject</code> are functions if the client requested a response. <code>accept()</code> returns a <em>Channel</em> for the interactive shell. <code>reject()</code> Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>exec</strong>(&lt; <em>mixed</em> &gt;accept, &lt; <em>mixed</em> &gt;reject, &lt; <em>object</em> &gt;info) - The client has requested execution of a command string. <code>accept</code> and <code>reject</code> are functions if the client requested a response. <code>accept()</code> returns a <em>Channel</em> for the command execution. <code>reject()</code> Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic. <code>info</code> has these properties:</p> <ul> <li><strong>command</strong> - <em>string</em> - The command line to be executed.</li> </ul> </li> <li><p><strong>sftp</strong>(&lt; <em>mixed</em> &gt;accept, &lt; <em>mixed</em> &gt;reject) - The client has requested the SFTP subsystem. <code>accept</code> and <code>reject</code> are functions if the client requested a response. <code>accept()</code> returns an <em>SFTPStream</em> in server mode (see the <a href=https://github.com/mscdex/ssh2-streams/blob/master/SFTPStream.md><code>SFTPStream</code> documentation</a> for details). <code>reject()</code> Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic. <code>info</code> has these properties:</p> </li> <li><p><strong>subsystem</strong>(&lt; <em>mixed</em> &gt;accept, &lt; <em>mixed</em> &gt;reject, &lt; <em>object</em> &gt;info) - The client has requested an arbitrary subsystem. <code>accept</code> and <code>reject</code> are functions if the client requested a response. <code>accept()</code> returns a <em>Channel</em> for the subsystem. <code>reject()</code> Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic. <code>info</code> has these properties:</p> <ul> <li><strong>name</strong> - <em>string</em> - The name of the subsystem.</li> </ul> </li> <li><p><strong>close</strong>() - The session was closed.</p> </li> </ul> <h3 id=channel>Channel</h3> <p>This is a normal <strong>streams2</strong> Duplex Stream (used both by clients and servers), with the following changes:</p> <ul> <li><p>A boolean property <code>allowHalfOpen</code> exists and behaves similarly to the property of the same name for <code>net.Socket</code>. When the stream&#39;s end() is called, if <code>allowHalfOpen</code> is <code>true</code>, only EOF will be sent (the server can still send data if they have not already sent EOF). The default value for this property is <code>true</code>.</p> </li> <li><p>A <code>close</code> event is emitted once the channel is completely closed on both the client and server.</p> </li> <li><p>Client-specific:</p> <ul> <li><p>For exec():</p> <ul> <li><p>An <code>exit</code> event <em>may</em> (the SSH2 spec says it is optional) be emitted when the process finishes. If the process finished normally, the process&#39;s return value is passed to the <code>exit</code> callback. If the process was interrupted by a signal, the following are passed to the <code>exit</code> callback: null, &lt; <em>string</em> &gt;signalName, &lt; <em>boolean</em> &gt;didCoreDump, &lt; <em>string</em> &gt;description.</p> </li> <li><p>If there was an <code>exit</code> event, the <code>close</code> event will be passed the same arguments for convenience.</p> </li> <li><p>A <code>stderr</code> property contains a Readable stream that represents output from stderr.</p> </li> </ul> </li> <li><p>For shell() and exec():</p> <ul> <li><p>The readable side represents stdout and the writable side represents stdin.</p> </li> <li><p><strong>signal</strong>(&lt; <em>string</em> &gt;signalName) - <em>boolean</em> - Sends a POSIX signal to the current process on the server. Valid signal names are: &#39;ABRT&#39;, &#39;ALRM&#39;, &#39;FPE&#39;, &#39;HUP&#39;, &#39;ILL&#39;, &#39;INT&#39;, &#39;KILL&#39;, &#39;PIPE&#39;, &#39;QUIT&#39;, &#39;SEGV&#39;, &#39;TERM&#39;, &#39;USR1&#39;, and &#39;USR2&#39;. Some server implementations may ignore this request if they do not support signals. Note: If you are trying to send SIGINT and you find <code>signal()</code> doesn&#39;t work, try writing <code>&#39;\\x03&#39;</code> to the Channel stream instead. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>setWindow</strong>(&lt; <em>integer</em> &gt;rows, &lt; <em>integer</em> &gt;cols, &lt; <em>integer</em> &gt;height, &lt; <em>integer</em> &gt;width) - <em>boolean</em> - Lets the server know that the local terminal window has been resized. The meaning of these arguments are described in the &#39;Pseudo-TTY settings&#39; section. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> </ul> </li> </ul> </li> <li><p>Server-specific:</p> <ul> <li><p>For exec-enabled channel instances there is an additional method available that may be called right before you close the channel. It has two different signatures:</p> <ul> <li><p><strong>exit</strong>(&lt; <em>integer</em> &gt;exitCode) - <em>boolean</em> - Sends an exit status code to the client. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> <li><p><strong>exit</strong>(&lt; <em>string</em> &gt;signalName[, &lt; <em>boolean</em> &gt;coreDumped[, &lt; <em>string</em> &gt;errorMsg]]) - <em>boolean</em> - Sends an exit status code to the client. Returns <code>false</code> if you should wait for the <code>continue</code> event before sending any more traffic.</p> </li> </ul> </li> <li><p>For exec and shell-enabled channel instances, <code>channel.stderr</code> is a writable stream.</p> </li> </ul> </li> </ul> <h3 id=pseudo-tty-settings>Pseudo-TTY settings</h3> <ul> <li><p><strong>rows</strong> - &lt; <em>integer</em> &gt; - Number of rows. <strong>Default:</strong> <code>24</code></p> </li> <li><p><strong>cols</strong> - &lt; <em>integer</em> &gt; - Number of columns. <strong>Default:</strong> <code>80</code></p> </li> <li><p><strong>height</strong> - &lt; <em>integer</em> &gt; - Height in pixels. <strong>Default:</strong> <code>480</code></p> </li> <li><p><strong>width</strong> - &lt; <em>integer</em> &gt; - Width in pixels. <strong>Default:</strong> <code>640</code></p> </li> <li><p><strong>term</strong> - &lt; <em>string</em> &gt; - The value to use for $TERM. <strong>Default:</strong> <code>&#39;vt100&#39;</code></p> </li> <li><p><strong>modes</strong> - &lt; <em>object</em> &gt; - An object containing <a href=#terminal-modes>Terminal Modes</a> as keys, with each value set to each mode argument. <strong>Default:</strong> <code>null</code></p> </li> </ul> <p><code>rows</code> and <code>cols</code> override <code>width</code> and <code>height</code> when <code>rows</code> and <code>cols</code> are non-zero.</p> <p>Pixel dimensions refer to the drawable area of the window.</p> <p>Zero dimension parameters are ignored.</p> <h3 id=terminal-modes>Terminal modes</h3> <table> <thead> <tr> <th>Name</th> <th>Description</th> </tr> </thead> <tbody><tr> <td>VINTR</td> <td>Interrupt character; 255 if none. Similarly for the other characters. Not all of these characters are supported on all systems.</td> </tr> <tr> <td>VQUIT</td> <td>The quit character (sends SIGQUIT signal on POSIX systems).</td> </tr> <tr> <td>VERASE</td> <td>Erase the character to left of the cursor.</td> </tr> <tr> <td>VKILL</td> <td>Kill the current input line.</td> </tr> <tr> <td>VEOF</td> <td>End-of-file character (sends EOF from the terminal).</td> </tr> <tr> <td>VEOL</td> <td>End-of-line character in addition to carriage return and/or linefeed.</td> </tr> <tr> <td>VEOL2</td> <td>Additional end-of-line character.</td> </tr> <tr> <td>VSTART</td> <td>Continues paused output (normally control-Q).</td> </tr> <tr> <td>VSTOP</td> <td>Pauses output (normally control-S).</td> </tr> <tr> <td>VSUSP</td> <td>Suspends the current program.</td> </tr> <tr> <td>VDSUSP</td> <td>Another suspend character.</td> </tr> <tr> <td>VREPRINT</td> <td>Reprints the current input line.</td> </tr> <tr> <td>VWERASE</td> <td>Erases a word left of cursor.</td> </tr> <tr> <td>VLNEXT</td> <td>Enter the next character typed literally, even if it is a special character</td> </tr> <tr> <td>VFLUSH</td> <td>Character to flush output.</td> </tr> <tr> <td>VSWTCH</td> <td>Switch to a different shell layer.</td> </tr> <tr> <td>VSTATUS</td> <td>Prints system status line (load, command, pid, etc).</td> </tr> <tr> <td>VDISCARD</td> <td>Toggles the flushing of terminal output.</td> </tr> <tr> <td>IGNPAR</td> <td>The ignore parity flag. The parameter SHOULD be 0 if this flag is FALSE, and 1 if it is TRUE.</td> </tr> <tr> <td>PARMRK</td> <td>Mark parity and framing errors.</td> </tr> <tr> <td>INPCK</td> <td>Enable checking of parity errors.</td> </tr> <tr> <td>ISTRIP</td> <td>Strip 8th bit off characters.</td> </tr> <tr> <td>INLCR</td> <td>Map NL into CR on input.</td> </tr> <tr> <td>IGNCR</td> <td>Ignore CR on input.</td> </tr> <tr> <td>ICRNL</td> <td>Map CR to NL on input.</td> </tr> <tr> <td>IUCLC</td> <td>Translate uppercase characters to lowercase.</td> </tr> <tr> <td>IXON</td> <td>Enable output flow control.</td> </tr> <tr> <td>IXANY</td> <td>Any char will restart after stop.</td> </tr> <tr> <td>IXOFF</td> <td>Enable input flow control.</td> </tr> <tr> <td>IMAXBEL</td> <td>Ring bell on input queue full.</td> </tr> <tr> <td>ISIG</td> <td>Enable signals INTR, QUIT, [D]SUSP.</td> </tr> <tr> <td>ICANON</td> <td>Canonicalize input lines.</td> </tr> <tr> <td>XCASE</td> <td>Enable input and output of uppercase characters by preceding their lowercase equivalents with &quot;&quot;.</td> </tr> <tr> <td>ECHO</td> <td>Enable echoing.</td> </tr> <tr> <td>ECHOE</td> <td>Visually erase chars.</td> </tr> <tr> <td>ECHOK</td> <td>Kill character discards current line.</td> </tr> <tr> <td>ECHONL</td> <td>Echo NL even if ECHO is off.</td> </tr> <tr> <td>NOFLSH</td> <td>Don&#39;t flush after interrupt.</td> </tr> <tr> <td>TOSTOP</td> <td>Stop background jobs from output.</td> </tr> <tr> <td>IEXTEN</td> <td>Enable extensions.</td> </tr> <tr> <td>ECHOCTL</td> <td>Echo control characters as ^(Char).</td> </tr> <tr> <td>ECHOKE</td> <td>Visual erase for line kill.</td> </tr> <tr> <td>PENDIN</td> <td>Retype pending input.</td> </tr> <tr> <td>OPOST</td> <td>Enable output processing.</td> </tr> <tr> <td>OLCUC</td> <td>Convert lowercase to uppercase.</td> </tr> <tr> <td>ONLCR</td> <td>Map NL to CR-NL.</td> </tr> <tr> <td>OCRNL</td> <td>Translate carriage return to newline (output).</td> </tr> <tr> <td>ONOCR</td> <td>Translate newline to carriage return-newline (output).</td> </tr> <tr> <td>ONLRET</td> <td>Newline performs a carriage return (output).</td> </tr> <tr> <td>CS7</td> <td>7 bit mode.</td> </tr> <tr> <td>CS8</td> <td>8 bit mode.</td> </tr> <tr> <td>PARENB</td> <td>Parity enable.</td> </tr> <tr> <td>PARODD</td> <td>Odd parity, else even.</td> </tr> <tr> <td>TTY_OP_ISPEED</td> <td>Specifies the input baud rate in bits per second.</td> </tr> <tr> <td>TTY_OP_OSPEED</td> <td>Specifies the output baud rate in bits per second.</td> </tr> </tbody></table> <h3 id=httpagent>HTTPAgent</h3> <h4 id=httpagent-methods>HTTPAgent methods</h4> <ul> <li><strong>(constructor)</strong>(&lt; <em>object</em> &gt;sshConfig[, &lt; <em>object</em> &gt;agentConfig]) - Creates and returns a new <code>http.Agent</code> instance used to tunnel an HTTP connection over SSH. <code>sshConfig</code> is what is passed to <code>client.connect()</code> and <code>agentOptions</code> is passed to the <code>http.Agent</code> constructor.</li> </ul> "}}]);