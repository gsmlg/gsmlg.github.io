// import { fromJS } from 'immutable';
// import { makeSelectChineseChessDomain } from '../selectors';

// const selector = makeSelectChineseChessDomain();

describe('makeSelectChineseChessDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
