// import { fromJS } from 'immutable';
// import { makeSelectNetworksDomain } from '../selectors';

// const selector = makeSelectNetworksDomain();

describe('makeSelectNetworksDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
