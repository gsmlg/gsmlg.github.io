// import { fromJS } from 'immutable';
// import { selectDistributedNodePageDomain } from '../selectors';

describe('selectDistributedNodePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
