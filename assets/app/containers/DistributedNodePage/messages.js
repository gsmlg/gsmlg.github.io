/*
 * DistributedNodePage Messages
 *
 * This contains all the text for the DistributedNodePage component.
 */
import { defineMessages } from 'react-intl';

const scope = 'app.containers.DistributedNodePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is DistributedNodePage container !',
  },
});
