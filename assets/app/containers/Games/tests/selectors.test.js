// import { fromJS } from 'immutable';
// import { makeSelectGamesDomain } from '../selectors';

// const selector = makeSelectGamesDomain();

describe('makeSelectGamesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
